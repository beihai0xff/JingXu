# 镜序 v0.2.4

macOS 14+ · Apple Silicon · 构建 13

- 修复单图底部胶片条的竖图偏右问题。横图、竖图、方图和全景图按原比例完整居中显示，不裁切、不拉伸。
- 保留／淘汰旗标独立固定在右上角，不影响图片布局。
- 包含 v0.2.3 的 RAW 格式识别修复及安全清理失效索引功能。

Debug / Release 构建及 Release 19 项回归通过，包含五种长宽比的实际 SwiftUI 渲染检查。本地构建 12 替换前后 538 项资源及来源、标注、相册、分析、导入记录一致。发布构建 13 仅更新版本信息。

## 安装与边界

临时签名，未经 Apple 公证，按预发布版本提供，文件名不含 test。完整下载 DMG 的 Gatekeeper 与沙盒 UI 人工验收尚未完成，不要关闭系统安全保护。

先退出旧程序、备份图库，再打开 DMG，把“镜序.app”拖到“应用程序”替换；停用其他目录的旧副本。Bundle ID、沙盒权限和图库路径不变，不新增数据库迁移，不自动清理记录或修改照片。签名变化可能要求重新授权来源，不要删除图库处理授权问题。

回退须使用匹配版本的数据备份。附 DMG 与 SHA-256 校验文件，不覆盖已有版本资产。
