# 镜序优化验证记录

## 浏览与选片优化：2026-09-12

基于 `main` 的 `ff893f3`（0.4.6）实施。用户随后要求发布到 GitHub，版本元数据更新为 0.5.0／build 26；发布提交与产物以 [GitHub Release](https://github.com/beihai0xff/JingXu/releases/tag/v0.5.0) 的流水线记录为准。下文记录本机实现验证，不代表安装或界面验收。

### 实现范围

| 阶段 | 已接入的行为 | 主要入口 |
| --- | --- | --- |
| A：可靠性与状态 | XMP 固定清单、仅原子创建；同键缩略图请求合并与独立取消；失败重试及重新授权；操作状态与匹配计数分离 | `XMPExporter`、`ThumbnailRequests`、`BrowseControls`、`OperationAvailability` |
| B：连续选片 | 0–5 评分、淘汰与前进、文本焦点保护；关键词草稿保存；批量评分／旗标／关键词／相册；字段级事务撤销 | `AnnotationEditing`、`AnnotationActions`、`LibraryKeyboardHandler`、`BatchSelectionBar` |
| C：完整浏览范围 | 200 项复合游标分页；中文输入与 300 ms 搜索去抖；草稿查询与已应用查询分离；跨页显式选择、跨页单图导航、MCP 完整目标解析 | `CatalogBrowsing`、`BrowsingActions`、`AutomationConnection` |
| D：双图比较 | 固定参考图与活动候选；相似组入口；同步物理像素缩放／归一化平移；两张活动大图及一张候选预取 | `ComparisonView`、`PreviewCanvas` |

网格只保留当前一页摘要，单图保留前后各 40 张摘要；复用现有 SQLite 索引、系统图像框架、缓存、调色及文件操作协调器，未增加依赖或数据库迁移。旧的 OFFSET 查询字段、XMP 替换策略和单用途键盘处理器已删除。

### 本次自动化验证

- 在仓库根目录执行 `zsh Scripts/build.sh check`，退出码 0：arm64 Debug／Release 构建、51 组 JingXuChecks 及 29 项发布脚本检查全部通过，无编译警告。
- 十万条合成数据按 200 项一页遍历 500 页，无重复或遗漏。每页同时读取完整匹配计数，P95 为 **9.0 ms**，低于 200 ms 目标；这是同机 SQLite 查询耗时，不是窗口渲染或用户感知延迟。
- 既有缓存失效检查（5,000 文件／1,000 ID）5 次中位数为 45 ms。MCP 在 `SWIFTNIO_STRICT=1` 下通过 100 次并发请求／关闭／重启竞争。
- `JingXuChecks --workflow-ui-fixtures` 生成成功，独立数据库回读为 406 张照片、4 个相似组成员及 1 个带目录身份的来源。素材全部为合成图片。
- `git diff --check` 通过，31 个本地文档链接已核对。

专项检查覆盖：

- XMP 新建与重复执行、竞争目标、悬空链接、RAW/JPEG 同目标冲突、标注变化、照片替换、取消与临时文件清理。
- 401 张批量标注及相册成员、关键词三个模式；字段撤销和重做、独立字段保留、撤销冲突、缺失目标及注入数据库失败的整批回滚。
- 十万条数据的完整游标遍历、分页与完整计数一致、同日期同名边界、反向导航、跨页 ID 解析、Unicode 字节排序、字面搜索、筛选隐藏锚点及空白末页返回入口。
- 缩略图同键只渲染一次、单个订阅取消不影响其他订阅；双画布不同比例的联动中心、物理像素缩放与清空图像。

### 实际界面验收边界

本次原生界面工具两次报告 Mac 已锁定且自动解锁失败，尚未运行隔离应用的交互验收。已请求手动解锁。不能将上述命令行回归或画布组件检查视为以下项目通过：

- 中文输入法组词、文本框和滑块焦点、菜单及快捷键、长按 Delete。
- 跨页批选后进入单图再返回、标注导致项目离开筛选、批量关键词及撤销交互。
- 双图缩放／平移、快速切候选、单侧失败、外接屏比例变化和实际内存曲线。
- Sandbox 重新授权、系统分享、真实 RAW 解码与多次切图。

`JingXuChecks --workflow-ui-fixtures` 提供仓库外的 406 张合成照片独立图库，包括损坏图片和相似组。运行界面须以 `JINGXU_UI_TEST_ROOT` 隔离数据库、缓存、日志及偏好；打包验证须使用独立应用标识和对应 Sandbox 容器。没有使用真实图库进行删除、移动、恢复或重算。

使用说明见 [浏览与选片](BrowsingAndCulling.md)。

## 历史记录：0.4.6 导入与后台可靠性

以下是本次浏览与选片改动之前的验证快照，保留作为历史记录，不作为本次应用打包或界面验收证据。

验证日期：2026-09-12。实现分支：`codex/import-and-background-reliability`。

本文记录实现阶段的验证快照；后续 0.4.6 发布的功能与验收边界见 [版本说明](Release-0.4.6.md)，源提交由发布流水线附入 GitHub Release。

## 已实现

- 导入预检与固定清单确认；目录、分组、复制和索引错误分别呈现，最近一次结果持久保存，重启后中断任务可重新预检。
- RAW/JPEG 及明确关联 XMP 成组避让；视频独立处理。检查已有编号候选，支持部分组补齐，防止重复导入产生额外副本。
- 普通与调色缩略图共用后台缓存，按照片直接失效、限制容量、回收旧修订，隔离清理前的请求。
- MCP 连接同步注册，请求任务由服务跟踪；关闭等待控制器、流与请求收尾后再关闭事件循环。
- 索引完成即刷新图库，分析期间节流刷新并保留操作互斥与标注保护。

使用规则见 [安全导入](SafeImport.md) 和 [缓存与后台任务](BackgroundWork.md)。

## 本次验证结果

- 在项目根目录执行 `zsh Scripts/build.sh check`：Debug／Release 构建、47 组 JingXuChecks、29 项发布脚本检查通过。
- 导入覆盖局部目录权限、模糊配对、两种 XMP、照片与视频同主名、编号空洞、重复重试、文件替换／消失、取消、部分提交、竞争目标、容量不足、数据库失败及 2,001 项完整范围。所有文件操作样本均为隔离测试材料；模拟 RAW 用于文件操作校验，不代表真实 RAW 解码验收。
- 缓存覆盖清理与写入竞争、重启后修订保护、容量淘汰、屏幕比例、写入失败。5,000 个缓存文件／1,000 个不匹配照片 ID，5 次失效耗时中位数 48 ms，低于 500 ms 验收目标；这是同机合成样本，不是实际界面耗时。
- `SWIFTNIO_STRICT=1` 下，100 次并发请求／关闭／重启通过，并覆盖初始化竞争及实际 HTTP SSE 流关闭；未出现向已关闭事件循环调度的警告。
- 独立源码副本通过 `zsh Scripts/build.sh adhoc` 的完整构建回归、应用签名与 DMG 校验。未覆盖已有安装包，未修改版本元数据，未合入 GitHub 或发布。

## 尚待完成的界面验收

已建立独立应用标识及 Sandbox 图库，使用合成 JPEG 准备导入材料。原生界面工具报告 Mac 已锁定且自动解锁失败，因此预检清单、部分完成、重试和分析期间浏览／筛选／评分的实际界面操作尚未验收。需要手动解锁后继续；命令行通过不替代这一结果。

未替换已安装应用，未使用真实图库或照片进行文件操作。
