# 镜序 0.4.3

构建 22，macOS 14+、Apple Silicon。

- 修复删除结果提示关闭后，左侧分类、来源目录及「包含子目录」可能一直处于禁用状态的问题。
- 禁用样式不再依赖未被观察的系统弹窗状态；实际切换仍检查弹窗，保留后台任务、确认清单、调色保存和 AI 操作期间的保护。
- 不更改数据库格式、删除算法或图库路径，不自动移动、删除或重新分析照片。

本包采用临时签名，未经 Apple 公证。安装前结束后台任务、退出旧应用并备份图库，再替换「应用程序」中的镜序。不要关闭系统安全保护。

此次仅准备本机修复包；GitHub 发布须另行执行。问题定位及检查范围见 [目录浏览说明](FolderBrowsing.md)。完整的删除后点击回归、跨设备 Gatekeeper 和 XCUITest 尚未验收，不将构建通过等同于这些系统交互验证通过。

本机已通过统一入口的 Debug／Release 构建、32 项应用回归、29 项发布脚本校验及临时签名打包。备份后完成本地覆盖安装，启动后的图库逻辑内容及文件操作日志与安装前一致。打包应用已检查分类切换、取消空清理确认页后直接切换目录；未执行真实照片删除。回归期间出现 SwiftNIO 已关闭 EventLoop 的调度告警，进程正常完成，尚未单独排查该告警。
