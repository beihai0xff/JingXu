# 镜序（JingXu）

镜序是一款面向摄影爱好者的原生 macOS 相机相册整理软件。它可以安全导入相机卡、非破坏式索引本地图库、读取 RAW/JPEG/视频元数据，并在本机完成质量分析和相似连拍整理。

## 已实现

- 相机卡或任意文件夹安全导入：SHA-256 校验、临时文件原子提交、重复跳过、同名文件安全加后缀、永不删除来源。
- 本地文件夹安全作用域书签与增量扫描；来源离线时保留目录记录。
- 系统支持的 RAW、JPEG、HEIC、PNG、TIFF 缩略图与元数据，视频索引和预览。
- RAW+JPEG 同名配对，按日期、相机、镜头、类型、评分、旗标、关键词和质量状态查询。
- 完全离线的清晰度、阴影、高光和 Vision Feature Print 相似连拍分析。
- 评分、保留/淘汰旗标、关键词、手动相册和质量建议审核。
- 显式 XMP sidecar 导出；默认跳过已有文件，确认后才替换。
- SQLite/GRDB WAL 目录、可重建的内存/磁盘缩略图缓存。
- 10 万条模拟目录查询性能校验。
- 确认后批量将淘汰照片移到系统废纸篓；持久化删除日志与中断恢复。
- 内置可调整大小的大图窗口，原图异步解码、EXIF 方向、适应窗口、100%、捏合缩放与拖动平移。

## 清理淘汰图片与放大查看

1. 选择来源或相册，设置搜索与筛选，点击工具栏“清理淘汰图片…”。后台操作进行中时此入口不可用，请先取消或等待完成。
2. 核对数量、总大小和展开的固定文件清单，再确认“移到废纸篓”。范围包括全部匹配的淘汰照片，不受网格 2,000 项显示上限影响。视频、未标记的配对文件和 XMP 不处理；同一文件由多个来源索引时，只有相关记录均在确认清单中且仍标为淘汰才处理。
3. 删除期间不能开始扫描或分析；取消仅停止后续文件。失败绝不退化为永久删除。删除日志位于图库目录的 `deletions.json`；重启时完成已移走文件的数据库清理。若中断恰好发生在系统移动与日志落盘之间，会保留记录并提示先在访达恢复原文件后重试，不推测删除成功。
4. 恢复文件请使用访达废纸篓；重新扫描后可重新索引，但评分、标签及相册成员不自动恢复。权限不足时请重新通过系统选择器授权来源目录。
5. 单击检查器图片或双击网格照片打开大图。按钮支持适应窗口、100%、放大与缩小；触控板可捏合，鼠标拖动平移，双击切换适应/100%，Esc 关闭。100% 将一个原图像素映射到一个屏幕物理像素。RAW 依赖系统解码器；仅能获取嵌入图时显示“嵌入预览”及实际分辨率。失败可点击重试。

## 直方图、来源管理与快捷标记

- 选中照片后，右侧预览下显示直方图；默认亮度，可切换 RGB 叠加。统计基于最长边 1,024 像素、转换到 sRGB 的系统解码预览，横轴为 0–255，不是 RAW 传感器数据。透明像素不计入；视频不支持，解码失败可重试。
- 选中网格照片或打开大图后，按 **X** 标记淘汰，按 **U** 取消旗标。重复按 X 不切换状态，不直接删除文件，也不自动跳到下一张。搜索框、标签输入和弹窗中不截获字母键。当前筛选隐藏照片时，大图仍可用 U 恢复旗标。
- 来源右键选择“移除来源…”，确认后仅清理该来源索引、评分、标签及相册成员关系，不修改硬盘照片或其他来源。手动相册右键“删除相册…”仅删除相册和成员关系。
- 重复添加同一目录会复用来源并重新扫描；符号链接会解析到真实目录，不按名称判重，父子目录仍可独立索引。离线且无法验证身份时不会猜测合并。
- 工具栏“整理重复来源…”展示可确认的重复组及冲突数量。确认后保留最早创建的来源，标签和相册成员取并集；评分和旗标采用最近修改的整条标注，时间相同则优先保留来源。文件身份冲突或未知时跳过整个组并报告。相关分析与缓存失效，重新扫描时重新分析，不自动改变旗标。
- 来源移除／合并前会创建独立 SQLite 在线备份，位于图库目录的 `Backups/`。备份失败不执行；数据库合并事务失败会回滚。备份不会自动清理。恢复整个数据库需先退出应用并保留当前数据库及 WAL 文件，再由熟悉 SQLite 的人员从备份恢复；目前没有一键恢复入口。
- 扫描、分析、文件清理与来源管理互斥。删除日志存在不明确的中断状态时，先按提示恢复或解决，才允许继续后台操作。
- “当前筛选无匹配结果”提供“清除筛选”，清除搜索、最低评分及旗标条件；切换来源仍保留这些条件。

## 运行

最低系统版本为 macOS 14。

```bash
swift run JingXuApp
```

也可以在安装完整 Xcode 后直接打开 `Package.swift`，选择 `JingXuApp` scheme 运行。App Sandbox 权限文件为 `JingXu.entitlements`。

## 自动化校验

当前机器只有 Command Line Tools，缺少 Xcode 附带的 XCTest，因此仓库提供了不依赖 XCTest 的自动化校验可执行程序：

```bash
swift run JingXuChecks
```

它覆盖目录迁移、筛选、标注、相册、增量扫描、RAW/JPEG 配对、元数据、质量建议、XMP、安全导入和 10 万条查询性能。

新增校验覆盖 2,005 项删除候选、搜索/相册/旗标隔离、视频与未标记文件保护、重复项、文件替换、来源文件缺失、模拟权限失败、取消、级联清理和中断恢复，以及 JPEG/HEIC 原图解码、EXIF 方向、损坏格式与加载取消。废纸篓测试使用临时目录和注入服务，不操作用户图库。真实 RAW 成功样本、缩放手势、100% 屏幕映射和打包应用内的实际废纸篓/Sandbox 操作仍需人工验收；当前环境无法运行 XCUITest。

直方图／来源管理新增校验包括黑白灰与 RGB 通道、透明像素排除、JPEG 统计和取消、目录重复添加、符号链接、同名不同目录、父子目录、标注及相册成员合并、备份内容、备份失败保护、数据库故障注入与事务回滚、重启持久化、身份冲突跳过及离线来源隔离。可运行 `swift run JingXuChecks --ui-fixtures` 生成独立的两张临时图片用于交互验收。

本次已在独立沙盒测试副本中验证系统选择器授权和临时图片索引。界面自动化连接随后中断，完整 X/U 输入焦点、大图窗口、来源移除／合并弹窗与 RGB 显示仍待人工完成；未运行 XCUITest，也未使用真实 RAW 成功样本。

## 打包

```bash
Scripts/package-app.sh
```

脚本从 `Packaging/Info.plist` 读取版本（当前 0.2.0，build 2），构建 Apple Silicon Release 程序，执行 Developer ID 签名及应用／DMG 双阶段公证，输出 `outputs/JingXu-0.2.0-macOS-arm64.dmg` 和 SHA-256 文件。DMG 内含“镜序.app”、“应用程序”快捷方式及升级说明；ZIP 只作为临时公证上传格式，不对外提供。

必须事先在钥匙串配置 Developer ID Application 证书及私钥、公证凭据，并设置 `DEVELOPER_ID_APPLICATION`（证书名称）、`DEVELOPER_TEAM_ID`、`NOTARY_KEYCHAIN_PROFILE`（钥匙串配置名称）。不要把私钥或密码写入仓库。缺少配置、签名／公证失败或同版本产物已存在时脚本停止，不降级为临时签名，也不覆盖已有产物。

从干净的、已合入 `origin/main` 的提交发布：完成 `Packaging/ReleaseChecklist.md` 验收，将 `RELEASE_ACCEPTANCE_COMMIT` 设置为该提交的完整 SHA，然后运行 `Scripts/publish-release.sh`。脚本需要 GitHub CLI 和发布权限，重新执行构建与回归、生成正式 DMG、打版本标签并创建 GitHub Release 草稿。只有核对下载产物及验收记录后才发布草稿。当前环境无有效 Developer ID 证书，尚不能生成或发布正式 DMG。

## 覆盖升级与异常恢复

退出全部旧版，打开 DMG，将“镜序.app”拖到“应用程序”并替换；停用“下载”等位置的旧副本。Bundle ID、沙盒权限和图库路径保持不变，不要删除应用容器。旧版尚未实现图库锁，因此不要在新版使用期间另行启动旧版。

启动先获取图库锁，检测已知迁移历史并检查完整性。有待执行迁移时，使用 SQLite 在线备份保存 WAL 中已提交数据、删除日志、版本清单和 SHA-256；完成备份后才迁移，迁移后再验证完整性与外键。升级不触发来源合并、不改变标注、不修改原照片。

失败会停在恢复界面，不创建替代空库，不启动扫描、分析或删除日志恢复。升级备份在图库目录的 `Backups/Upgrades/`；通过“从升级备份恢复”选择包含 `manifest.json` 的目录，确认后先保全当前数据库及伴随文件，再恢复备份。恢复中断的意图保存在 `restore-state.json`，未解决前阻止打开图库，重试必须选择相同备份。恢复后可重试升级或退出并使用与备份匹配的旧版。恢复会回退备份后的图库变更，不回退或修改原照片。

旧库 v1／v2／v3、备份失败、未来版本拒绝、图库锁、WAL 在线备份、迁移故障及恢复中断均使用独立测试库校验。真实 DMG 替换、首次正式签名容器授权、Gatekeeper 和完整键盘交互仍须在发布前完成；自动化校验不能替代这些验收。

## 数据与隐私

- 目录数据库：`~/Library/Application Support/JingXu/Catalog.sqlite`
- 缩略图缓存：`~/Library/Application Support/JingXu/Thumbnails/`
- 不使用账户、网络请求、云服务或遥测。
- 索引和分析不修改来源；只有明确确认的淘汰照片清理、导入目标和 XMP 导出位置会修改文件系统。

## 工程结构

- `Sources/JingXuCore`：数据模型、GRDB 目录、扫描、导入、元数据、缩略图、质量分析和 XMP。
- `Sources/JingXuApp`：SwiftUI 三栏图库、导入向导、过滤、检查器和设置。
- `Tests/JingXuChecks`：可在 Command Line Tools 环境运行的单元、集成与性能校验。

Apple Photos 接管、照片编辑、人脸/内容识别、地图和云同步不在当前 MVP 范围内。正式签名、公证及 GitHub 发布流程已提供，但在证书配置和发布验收完成前不发布正式版。
