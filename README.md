# 镜序（JingXu）

面向摄影爱好者的原生 macOS 相册整理工具。从相机卡导入照片，浏览 RAW 与 JPEG，挑选、标记、非破坏式调色并导出成片，再按拍摄日期或指定目录整理文件。

**本地运行 · 无需镜序账户 · 索引不修改原片**

默认在本机分析和渲染；可选启用 [Codex 调色连接](Documentation/CodexColorControl.md)，把所选照片的缩小预览交给 AI 助手进行调色判断。

[下载安装](#安装) · [功能介绍](#功能介绍) · [快速上手](#快速上手) · [开发指南](AGENTS.md)

## 安装

### 系统要求

- macOS 14 Sonoma 或更新版本。
- Apple Silicon Mac（M 系列芯片）；当前提供的安装包不支持 Intel Mac。
- RAW 解码依赖 macOS 对相机型号和格式的支持。

> 当前发布通道为 ad-hoc 签名、未经 Apple 公证的预发布版，可能被 Gatekeeper 拦截。请以对应 Release 说明为准；若系统拒绝启动，不要关闭系统安全保护。

### 方式一：Homebrew

已安装 Homebrew 的用户，在终端运行：

```sh
brew tap beihai0xff/jingxu https://github.com/beihai0xff/JingXu.git
brew install --cask beihai0xff/jingxu/jingxu
```

安装后从「应用程序」打开「镜序」，或运行：

```sh
open /Applications/镜序.app
```

### 方式二：下载 DMG

1. 打开 [GitHub Releases](https://github.com/beihai0xff/JingXu/releases)，选择所需版本，阅读该版本说明。
2. 下载附件中的 DMG；未公证预发布版文件名为 `JingXu-<版本>-test.<构建号>-macOS-arm64.dmg`。
3. 打开 DMG，将「镜序.app」拖入「应用程序」。
4. 从「应用程序」启动镜序。

Release 同时提供 `.sha256` 文件。将它与 DMG 放在同一目录，可在该目录执行校验（替换为实际下载文件名）：

```sh
shasum -a 256 -c 'JingXu-<版本>-test.<构建号>-macOS-arm64.dmg.sha256'
```

### 更新已有安装

更新前先完成或安全取消后台任务，解决未完成的归档／移动恢复提示，备份图库并退出镜序。

0.4.1 增加完整 v4 旧图库（包括 0.2.6）的单向转换：先创建独立在线备份及日志快照，再事务添加调色表并设置当前格式标识，保留所有旧记录。未知、更早、不完整或较新结构仍拒绝打开；不创建替代空库。详见 [迁移说明](Documentation/LegacyCatalogUpgrade.md)。

**Homebrew 安装：**

```sh
brew update
brew upgrade --cask beihai0xff/jingxu/jingxu
```

**DMG 安装：**下载新版本，将「镜序.app」拖入「应用程序」并替换旧版。保留原有应用容器和图库目录，不要同时运行新旧版本。

镜序目前没有应用内自动更新。手动安装后希望改由 Homebrew 管理，可参考 [Homebrew 安装与接管说明](Documentation/Homebrew.md)。

## 功能介绍

以下介绍当前仓库实现的功能；下载包包含的具体内容以对应 Release 说明为准。

### 安全导入与本地图库

- **从相机卡或文件夹导入**：预检并确认清单后复制，SHA-256 校验、RAW/JPEG 与配套 XMP 成组避让，重复文件跳过；保留失败结果并可重新预检重试，永不删除来源文件。[安全导入](Documentation/SafeImport.md)
- **直接添加已有照片文件夹**：在原位置建立索引，无需再复制一份照片；支持增量扫描，外接磁盘离线时保留来源记录。
- **按目录浏览**：在来源目录树中逐层展开，点击文件夹查看；默认包含子目录，也可只看当前层。目录计数与离线索引保留，切换目录不清除搜索和评分筛选。[目录浏览说明](Documentation/FolderBrowsing.md)
- **多格式浏览**：支持系统可解码的 RAW、JPEG、HEIC、PNG、TIFF，以及视频索引和预览；自动关联同名 RAW + JPEG。
- **元数据查看**：查看拍摄时间、相机、镜头、尺寸等信息，结合搜索、评分和旗标筛选照片。

### 看图与选片

- **单图查看**：双击进入大图浏览，默认收起侧栏、搜索和整理工具，只保留一行看图控制；支持适应窗口、100% 查看、捏合缩放和拖动平移，全屏时同样生效。点击信息按钮查看文件名、尺寸、评分和标记。
- **胶片栏导航**：底部为可收起的紧凑缩略图栏，保留完整画面比例，点击或使用方向键切图；悬停查看文件名。
- **评分和整理**：星级、未标记／淘汰旗标、关键词、手动相册，便于分轮筛选。
- **直方图**：查看亮度或 RGB 分布，统计基于解码预览，不代表 RAW 传感器原始数据。
- **XMP 导出**：显式导出标注到 sidecar 文件，默认跳过已有 XMP，替换需确认。

| 快捷键 | 操作 |
| --- | --- |
| `Delete` / `⌫` | 标记淘汰，保存成功后跳到下一张照片；也支持前向删除键 `⌦`，不直接删除文件 |
| `U` | 取消当前照片旗标，停留在当前照片 |
| `←` / `→` | 在单图模式查看上一张／下一张，首尾不循环 |
| `Esc` | 返回网格 |

输入文字或显示弹窗时不应触发选片快捷键。Delete 在网格和单图模式均跳过视频、按当前照片顺序前进；最后一张不循环，保存失败不跳转，长按不会连续淘汰。单图导航基于进入时的当前网格照片，最多 2,000 张，不包含视频。

### 非破坏式调色与成片

- 单图点击「调色」，调整曝光、对比度、高光、阴影、白色、黑色、白平衡、饱和度和鲜艳度，支持自动保存、原图对比、重置及会话撤销／重做。
- 网格与胶片栏显示已保存的效果，原片质量诊断和成片直方图分别展示。
- 支持自建预设及 Lightroom XMP 基础参数的严格导入。含曲线、HSL、蒙版、相机配置或未知效果字段的文件会拒绝，不能承诺与 Lightroom 出片一致。
- 选择照片后可按明暗、白平衡、色彩分组批量应用；先看清单、备份后提交，并可撤销本次批量调色。
- 导出原尺寸 sRGB JPEG（92%）、PNG 或 16 位 TIFF，目标重名跳过。RAW 使用系统完整解码，不以嵌入预览替代原片导出。

详见 [调色、预设与导出](Documentation/ColorEditing.md)。原文件始终保持不变，调色参数属于图库数据，不能当作缓存删除。

### 整理照片文件

- **按日期归档**：根据 EXIF 拍摄日期，将照片移入各自来源下的 `年份/YYYY-MM-DD/` 目录；关联 RAW、JPEG 和明确配套的 XMP 一同处理。此操作检查整个图库，不受当前网格筛选限制。[归档说明](Documentation/DateArchive.md)
- **多选与分享**：⌘ 点击增减、Shift 连选，或使用「批量选择」勾选；通过 macOS 系统菜单分享原片或调色 JPEG，保留 GPS，分享目标始终由用户选择。[分享说明](Documentation/PhotoSharing.md)
- **批量移动**：选中照片后移动到同一磁盘的指定目录；支持来源内或跨来源移动，保留评分、标签、相册关系和分析结果。[移动说明](Documentation/BatchMove.md)
- **清理淘汰照片**：先查看固定文件清单，确认后移到系统废纸篓；失败不会改为永久删除。
- **清理失效索引**：对已确认从磁盘消失的文件，备份后移除目录记录；离线或权限不足的来源保守跳过。
- **来源与相册管理**：移除来源或删除手动相册不删除原照片；支持检查并整理重复来源。

归档和批量移动会改变真实文件路径，只支持同一文件系统内移动，不覆盖已有文件；重名时按组添加序号。执行前展示清单并备份图库，提供中断恢复与最近任务撤销。照片本身不会因此获得备份，其他软件中的旧路径也不会自动更新。

### 本地质量分析与相似连拍

在本机分析纹理清晰度、明暗分布和相似连拍，辅助人工挑选照片；质量诊断、曝光统计与连拍信息分别展示。

**质量分析 v2 的新模糊报警当前保持关闭**，人工校准和独立验证尚未完成，不承诺准确率。无法可靠判断的照片不会被当作质量正常；重算保留人工审核和标注。详见 [质量分析 v2](Documentation/QualityAnalysisV2.md)。

## 快速上手

1. **添加照片**：已有本地图库，点击「添加文件夹…」授权并索引；需要从存储卡复制照片，选择「从相机卡导入」。
2. **浏览与挑选**：等待扫描，选择来源，双击照片查看细节；用星级、`Delete` 和 `U` 标记，按条件筛选。
3. **建立相册**：创建手动相册，将选中的照片加入，或添加关键词方便查找。
4. **按需整理**：需要改变磁盘目录时，再使用「按日期重新归档…」或「移动到目录…」，核对清单后执行。
5. **最后清理**：确认淘汰标记无误后，打开「清理淘汰图片…」，检查文件范围并移到废纸篓。

日常浏览、索引和标记不会修改原照片。导入、移动、清理和 XMP 导出各自有明确的文件操作入口。

## 数据与隐私

镜序无需账户，不使用网络请求、云服务或遥测，照片和分析结果留在本机。图库数据库保存索引、标注、调色参数与预设，缩略图缓存可以重建；它们不能替代原照片备份。

图库位于应用的 `Application Support/JingXu/` 下，沙盒版位于应用容器内；可在应用中查看实际图库位置。更新应用时保留这些数据。如果启动出现恢复提示，按提示处理，不要通过删除数据库或未完成任务日志来绕过。

## 开发与更多文档

开发需要 macOS 和支持 Swift 6.1 的工具链。在仓库根目录运行：

```sh
zsh Scripts/build.sh check
```

该入口与 CI 共用，执行版本与发布脚本检查、Debug / Release 构建和完整回归。生成临时签名测试包使用 `zsh Scripts/build.sh adhoc`；配置 Developer ID 和公证凭据后，使用 `zsh Scripts/build.sh release` 生成正式签名包。安装包输出到 `outputs/`，详见[打包与发布](Documentation/ReleasePipeline.md)。

本地启动界面使用 `swift run JingXuApp`，也可以通过 Xcode 打开 `Package.swift`。启动会访问默认图库，开发验收请使用隔离环境。

- [Agent 开发指南](AGENTS.md)：模块入口、实现约定、数据安全与验证方法。
- [Homebrew 安装与更新](Documentation/Homebrew.md)：接管手动安装、更新渠道与限制。
- [缓存与后台任务](Documentation/BackgroundWork.md)：索引后浏览、分析进度与缩略图回收。
- [日期归档](Documentation/DateArchive.md) / [批量移动](Documentation/BatchMove.md)：操作范围、配套文件、恢复与撤销。
- [质量分析 v2](Documentation/QualityAnalysisV2.md)：诊断边界、重算与人工校准。
- [打包与发布](Documentation/ReleasePipeline.md) / [发布检查清单](Packaging/ReleaseChecklist.md)：维护者构建和发布流程。
